# Shared PowerShell Area

This area is reserved for reusable PowerShell assets shared across deployment categories.

Planned contents:

- shared modules
- helper functions
- orchestration support code that is not tied to a single deployment toolchain
