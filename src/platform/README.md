# Shared Platform Code

This directory is reserved for reusable code shared across deployment categories.

Planned areas:

- PowerShell modules
- validation and preflight logic
- orchestration helpers
- shared naming, manifest, and profile handling

The first expected migration candidate is the current `HVLab.Automation.psm1` module and the preflight validator from the existing Hyper-V lab implementation.
